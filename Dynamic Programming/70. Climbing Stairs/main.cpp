#include <iostream>

using namespace std;

class Solution {
public:
    int climbStairs(int n) {
        
    }
};

int main(void) {
    int steps;
    cin >> steps;
    Solution sol;
    cout << sol.climbStairs(steps) << endl;
    return 0;
}